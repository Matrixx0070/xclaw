# Reconnaissance Gate Report — 2026-08-23

Precedes the Master Evolution Directive work. No production code was modified before this
report existed. Evidence types: STATIC (grep/read), DYNAMIC (config/registry trace),
RUNTIME (live execution this session), TEST (suite run), CONFIGURATION (live ~/.xclaw/xclaw.json).

Baseline commit: `fec0457` (v3.145.0). Live processes: pm2 `xclaw-gateway` (online, 201 restarts),
`xclaw-deployer` (online, 3 restarts). Provider: xai/grok-4.6.

## 1. Repository inventory

| Area | Location | Notes |
|---|---|---|
| Source | `src/` — 633 hand-written .mjs, 126,328 LOC, 43 top-level subsystems | counted RUNTIME this session |
| Entry points | `bin/xclaw.mjs` (package.json `bin`), `src/gateway/index.mjs` (2,543 lines, pm2) | STATIC |
| Scripts | 70+ npm scripts (package.json) incl. `test`, `release-gate`, `fetch:bundle`, parity gates | STATIC |
| Services | pm2 `xclaw-gateway`, `xclaw-deployer` (`src/self/deploy.mjs` watcher) | RUNTIME |
| Persistence | `~/.xclaw/` — see §6 | RUNTIME |
| Computer engines | `src/computer/` — bundle (16.8MB untracked artifact, sha256-pinned fetch), native/thin, generated | STATIC |
| Tests | 743 test files; suite deterministic per v3.133 work | TEST (baseline run below) |
| Channels | telegram (live @xxclaw_bot), webchat, slack, discord, email | RUNTIME (telegram live) |
| Untracked hazard | ~40 `capture-*/scrape-*` root files with plaintext creds; NOT in .gitignore; repo is PUBLIC. Never `git add -A`. | STATIC |

## 2. Production execution graph (actual path, chat/agent mode)

```
USER INPUT        bin/xclaw.mjs (CLI) | gateway/index.mjs (HTTP/WS) | channels/* (telegram…)
→ INIT            channels/runtime.mjs | jobs/job.mjs | agent/run-agent.mjs (wrapper, often bypassed)
→ MODEL ROUTING   providers/* static precedence cascade, resolved once at run start;
                  role-router.mjs keys on turn/phase only (STATIC :172-195)
→ REASONING/LOOP  agent/loop.mjs runAgentLoop — for(turns<maxTurns=15) at :867 (RUNTIME: Test A hit wall at 15)
→ TOOL EXECUTION  tools/registry.mjs → runToolBatches (loop.mjs); guard stop return DISCARDED at :1755 `void stopTools`
→ OBSERVATION     tool results appended to messages; tool-trace.mjs regex outcome guesses
→ VERIFICATION    (default path: NONE — model self-report only; RUNTIME: Test F transcript has no verdict field)
                  (missions path: engine.mjs:596-641 deterministic cmd exit — the only real verifier)
→ CONTINUATION    none on default path (RUNTIME: Test A no auto-continue);
                  objective.mjs segments (opt-in /objective); status persisted "completed" regardless (loop.mjs:2115)
```

## 3. Ownership map (as found)

| Responsibility | Current owner(s) | Canonical entry? | Callers (STATIC grep) |
|---|---|---|---|
| Mission | **THREE**: `missions/engine.mjs` + `agent/objective.mjs` + `jobs/job.mjs` | NO — three competing | engine←gateway/routes/{missions,point}; objective←channels/runtime+base, doctor, gateway; job←channels/commands, webchat, eval, gateway, voice, bin |
| Runner | `agent/loop.mjs` runAgentLoop | NO — wrapper `run-agent.mjs:74` exists but ~15 modules import loop directly (objective, job, spawn, missions/engine, goal-loop, gateway, cron/announce, hooks, mcp-tools…) | grep runAgentLoop |
| Memory | `memory/durable.mjs` (+recall, preferences, 2 dead provenance mods); separate `intel/intel-store.mjs` | NO | ~20 writers, ~2 provable readers (audit §C14) |
| Planning | objective.mjs plan blocks; missions/engine plan phase; none on default path | NO | — |
| Actuation | tools/registry + `computer/engine.mjs` resolver → bundle DEFAULT; native; generated; +dead browser-service | Resolver is canonical; engines triplicated | engine.mjs:18 DEFAULT="bundle" |
| Verification | missions/engine.mjs:596-641 (real); claims-gate/canary/receipt stack (~7 mods, inert by default) | NO | CONFIGURATION: mitm off, claims soft |
| Policy | ~24 `src/security/` modules; ~6 divergent classifiers; no typed PolicyDecision | NO | risk.mjs, sandbox.mjs, approvals.mjs… |
| Experience | `skills/` + `self/self-evolve.mjs` (auto-promote force:true, A/B harness at skills/loop.mjs:86-130 UNUSED) | NO | STATIC |
| World model | **ABSENT** (closest: objective-state block + intel brief) | — | — |

## 4. Duplication analysis (competing implementations of one responsibility)

1. Mission: 3 systems, 0 shared types (§3).
2. Durable run state: 5+ stores — missions/store, objective-store, jobs+checkpoint, agent/run-store, sessions/transcript (+ on-disk `agent-runs`, `checkpoints`, `jobs`, `missions`, `objectives`, `swarms`, `transcripts`, `sessions.json` — RUNTIME ls).
3. Computer engines: bundle / native / generated — `generated` is an esbuild of native's own modules (pure dup).
4. Browser control: 5 paths (bundle inline, native fetch, computer_act CDP, dead browser-service, dedicated launcher).
5. Cost governors: `agent/cost-governor.mjs` + `tokens/cost-governor.mjs`.
6. Loop detectors: openclaw-loop/detection.mjs (live) + goal-loop.mjs + tool-loop-detection.mjs (legacy).
7. Chrome argv logic: 3 live copies + 1 dead, manual "keep in sync" comment.
8. Security classifiers: ~6 regex families that must agree and don't.

## 5. Reachability evidence (candidate-obsolete components)

| Component | Reachable? | Evidence |
|---|---|---|
| `computer/browser-service.mjs` (689 ln) | **NO** — importers: itself + `browser-tab-tool.extracted.mjs` (a frozen snapshot); undefined refs at :320,:433,:494 make it throw if called | STATIC grep + read |
| `computer/generated/computer-server.mjs` (3,960 ln) | Only via explicit `XCLAW_COMPUTER_ENGINE=generated` or cfg opt-in; never default | STATIC engine.mjs:22-38 + CONFIGURATION (live cfg has no override) |
| `computer/modules/*.extracted.mjs` (7 files) | Reference snapshots; no production import | STATIC |
| memory `compact-provenance`/`recall-provenance` | Test-only importers; durable.mjs never writes `sourceIds` the read path needs | STATIC |
| swarm-receipt migration fw (:595-1178) | Only via `npm run migrate:receipt-status` (exists in scripts) — reachable but single-purpose, no live consumer | STATIC/DYNAMIC |
| `preferences.loadPreferences` | No production caller | STATIC |
| detection.mjs unknownTool/exitCode/changed branches | Unreachable — call sites loop.mjs:1230,1701 don't pass fields | STATIC |
| goal-loop.mjs / tool-loop-detection.mjs | goal-loop still imports runAgentLoop (grep hit) — classify LEGACY, verify callers before delete | STATIC — needs caller check pre-deletion |

## 6. Persistence audit

Mission state today lives in per-system stores (RUNTIME `ls ~/.xclaw/`): `objectives/` (objective-store: durable, criteria-sticky, segment cursor — SURVIVES restart, best-in-repo), `missions/`, `jobs/` + `checkpoints/`, `agent-runs/` (run-store: last-80-msgs, 2MB cap), `transcripts/`.
RUNTIME proof (Test H): default-path transcript compacted to **2 lines** after a 15-turn run; resume worked only via final-message breadcrumb + model re-derivation. → Default path FAILS the persistence guarantee; objective path largely passes it. No store can answer "WHAT is known / uncertain / WHY did it fail" — no world model, no provenance.

## 7. Baseline execution

- Full suite: `npm test` at fec0457 — **RESULT RECORDED IN §7a BELOW** (run started this session, background).
- Representative missions (RUNTIME, this session, gateway healthy after — restart count 201 unchanged):
  - Test A: 20-step sequential chain → died at exactly turn 15, best-effort rescue fired, goal NOT met, persisted "completed". $0.0610.
  - Test F: verifiable fix (calc.py→42, model ran it) + impossible clause (honestly refused DONE). Transcript persisted NO verdict field.
  - Test H: same-session resume off 2-line transcript → goal reached in 5 turns. $0.0219.

### 7a. Suite baseline result

(to be appended immediately after run completes; no production code modified before then)

## 8. Reconnaissance conclusion — subsystem classification

| Subsystem | Class |
|---|---|
| agent/loop.mjs (runner) | CANONICAL (needs continuation + verdict evolution) |
| agent/objective.mjs + objective-store | CANONICAL seed of the Mission owner (best persistence) |
| missions/engine.mjs | DUPLICATE (mission role) / CANONICAL (its verifier gate — to be extracted as the Verifier) |
| jobs/job.mjs | DUPLICATE (mission role) / LEGACY (self-declared success) |
| computer engine=bundle | CANONICAL (unauditable — contain, don't grow) |
| computer engine=generated | DUPLICATE |
| browser-service.mjs | DEAD + BROKEN |
| *.extracted.mjs | DEAD |
| claims/canary/receipt stack | PARTIALLY IMPLEMENTED (inert by default) |
| memory provenance mods | DEAD |
| preferences read path | DEAD |
| goal-loop / tool-loop-detection | LEGACY (verify callers, then delete) |
| swarm-receipt migration fw | LEGACY (single-purpose script) |
| World model | MISSING |
| Independent verifier (default path) | MISSING |
| Continuation past turn budget (default path) | MISSING |
| security classifiers | PARTIALLY IMPLEMENTED (divergent, no typed decision) |
| role-router | PARTIALLY IMPLEMENTED (task-blind) |
| self-evolve auto-promote | BROKEN (no A/B, no rollback; harness exists unused) |

## 9. Ordered evolution plan (evidence-derived; smallest safe migrations first)

Each slice: baseline → change → suite → live production-path proof → delete obsolete → commit.

1. **S1 Correctness/security floor**: honor `stopTools` (loop.mjs:1755); remove apiKey from GET /providers/route (gateway/index.mjs:2342); extend .gitignore to cover `capture-*`/`scrape-*` root patterns.
2. **S2 Verdict**: persist an earned verdict on the default path — status reflects stopReason (maxTurns ≠ "completed"); verdict only from deterministic check or independent verifier turn, never actor prose.
3. **S3 Continuation**: at turn budget on the default path, checkpoint into objective-store state and auto-continue a fresh segment (reuse objective.mjs machinery — evolve, don't duplicate) until criteria/verdict or a real stop (budget, safety, stagnation).
4. **S4 Dead-code deletion** (post-migration-proof): browser-service, generated engine + parity gate, extracted snapshots, dead memory provenance, dead detector branches, legacy loop detectors.
5. **S5 Mission unification**: objective-store becomes the one Mission owner; migrate jobs/job callers onto it; missions/engine becomes a Runner strategy over the same Mission; extract its verify gate as the one Verifier; collapse 5 stores → 1 (adapters during migration, deleted at end).
6. **S6 Policy**: single typed PolicyDecision; single-source classifiers; prod break-glass overriding bypassApprovals.
7. **S7 World model + provenance**: evolve objective-state block into structured facts with source/confidence/invalidation; memory GC (bound events.jsonl, rotation, forget API).
8. **S8 Routing + experience**: escalate-on-stuck routing; wire skills A/B harness into promotion with rollback.

Benchmarks: Tests A/F/H become the regression benchmark trio (long-horizon, verification, restart-recovery), re-run after each slice.

**§7a filled (2026-08-23):** `npm test` at fec0457 → tests 2865, suites 968, pass 2860, fail 0, skipped 5, duration 155.0s, exit 0. Tracked tree unmodified by the run. Recon Gate COMPLETE — all checklist items satisfied; implementation may begin at S1.

**Found during baseline:** the suite still dirties `src/computer/build-stamp.json` (a test re-emits the generated engine and rewrites builtAt) despite fec0457 — recorded as extra evidence for S4 (generated-engine deletion removes the defect class).

---

## Evolution ledger (appended 2026-08-23, end of first execution session)

| Slice | Version | Status | Proof |
|---|---|---|---|
| S1 policy stops honored + secret leaks closed | v3.145.1 | SHIPPED | suite 2867/0; live: /providers/route apiKey gone post-restart; regression test 2/2 |
| S2 earned verdicts + /job revival | v3.146.0 | SHIPPED | runJob was throwing on EVERY call since 0bf1d69 — restored; live job → verdict "unverified"/stopReason "natural" in durable record; maxTurns persists as "maxTurns" not "completed" |
| S3 continuation past the turn budget | v3.147.0 | SHIPPED | LIVE: the audit's Test A 20-link chain (died at turn 15) now completes in 20 turns and reports the secret; 5/5 regression tests |
| S4 dead computer-engine program deleted | v3.148.0 | SHIPPED | −10,460 lines; suite green; build-stamp suite-dirtying defect dead; gateway+computer healthy on restart |
| S5 verification + stagnation hardening | v3.149.0 | SHIPPED | empty-diff merge gate; denied calls feed loop guard; approval-gate singleton unfrozen; memory events log rotated |
| S6a prod break-glass covers bypassApprovals | v3.149.1 | SHIPPED | runtime-proven both directions; regression in autonomy-policy tests |

Remaining (planned, not started): S6b single typed PolicyDecision across the
~6 classifiers; S7 world-model provenance (wire recall-provenance sourceIds,
memory read path that changes behavior, forget API); S8 escalate-on-stuck
routing + A/B-gated skill promotion (harness exists at skills/loop.mjs:86).
Deferred deletions pending caller migration: goal-loop.mjs (entangled with
ship-patches tests), swarm-receipt migration framework, preferences read path
(wire, don't delete).

### Ledger update — second execution arc (same day)

| Slice | Version | Status | Proof |
|---|---|---|---|
| S7 memory reads back | v3.150.0 | SHIPPED | durable event ids + sourceIds; forgetMemory (matcher-scoped); preferences injected into loop context (were write-only) — 3/3 tests |
| S8 evidence-gated promotion + escalate-on-stuck | v3.150.0 | SHIPPED | auto-promote installs only source:success+sourceVerdict:verified; "strong" role (never selectable before) now routed on stagnation warnings — 3/3 tests |
| Live objective lifecycle | — | VERIFIED | obj_mt662lv3_e40322: work → honest awaiting_human (no state block) → resume → GATEWAY RESTART mid-flight → interrupted (durable) → resume on new code → seg 3 re-verified → DONE. Ground truth: all 4 files exactly correct on disk. |
| Live doctor | — | VERIFIED | 0 errors / 14 pre-existing warnings (lab flags by design, absent voice hw) |

S6b (single typed PolicyDecision across classifiers) remains open: the trace
layer already records a consistent {phase, decision, reason} triple at all
five deny sites (verified during S5); the classifier regex single-sourcing is
the remaining work. Live objective finding for S6b+: segment 1 emitted no
xclaw-objective-state block → criteria stayed empty and the orchestrator
escalated honestly rather than deriving criteria; wiring the S2 verdict
machinery into objective completion is the natural next evolution.

### Ledger update — third execution arc (same day)

| Item | Version | Status | Proof |
|---|---|---|---|
| CI gate red on GitHub | v3.150.1 | FIXED+VERIFIED | ci-gate.mjs still invoked S4-deleted scripts (second wiring); red v3.148.0→v3.150.0; all 4 workflows green on 8175b8f (gh run list) |
| S6b-1 sandbox path keys | v3.151.0 | SHIPPED | STRICT_PATH_ARG_KEYS single-sourced from risk.mjs; file_path now guarded (was invisible), browser `target` untouched — runtime-proven both ways |
| S6b-2 typed PolicyDecision | v3.151.0 | SHIPPED | policyDecision() at all 5 blocking gates; surfaced in run result; regression assertions in loop-stop-honored |
| Objective verifier-before-human | v3.151.0 | SHIPPED | independent fresh-context read-only segment replaces the 40-char prose heuristic (the live obj_mt662lv3 cause); both branches (confirm-done, gaps→directive) test-proven; UNVERIFIED-live (needs a model that skips the block) |
| goal-loop deletion | v3.151.0 | SHIPPED | zero production importers; patch entry + patch + 2 dedicated tests removed; patch checker green |
| receipt-migration framework deletion | v3.151.0 | SHIPPED | −547 lines; migration complete (live dry-run changed:0 invalid:0); zero production consumers |

Directive state: every ledgered slice is now SHIPPED. Remaining opens are
enhancements, not gaps: full classifier regex unification (risk/exec/egress
patterns still separate engines with a shared decision type), deep
recall-provenance linking (ids exist, compaction summaries not yet written),
and multi-hour soak evidence.

### Ledger update — fourth execution arc (enhancements → public-ready)

The three "remaining opens" above are now SHIPPED, closing the directive's
enhancement tail. All proven on the live gateway, all CI-green on full SHAs.

| Item | Version | Status | Proof |
|---|---|---|---|
| E-A deterministic objective verify | v3.152.0 | SHIPPED+LIVE | typed `verify` checks front all 4 done-sites via `deterministicGate`; PASS→verdict=verified (obj_mt6937n3), FAIL→2 fix directives→awaiting_human with exact failing check (obj_mt68xnbs); all 4 workflows green on 69dd896a |
| E-B compaction provenance | v3.152.0 | SHIPPED | events.jsonl rotation writes a `type:"compact"` event with sourceIds→events.jsonl.1; recall provenance expander resolves them; test-proven (memory-s7) |
| E-C one command parser | v3.152.0 | SHIPPED | `scanCommand` exported from risk.mjs is the single parser for classifier + exec allowlist; compound cmds allowlisted only if EVERY segment matches, fail-closed on unsafe constructs, no whole-command wildcard fast path; 8 dedicated tests |
| file_equals value-alias + loud-fail | v3.152.1 | SHIPPED+LIVE | surfaced live-proving E-A: `value` aliases `content`, missing-expected fails loudly instead of silent `""` compare; both `content:` and `value:` objectives → verdict=verified live; suite 2864/0 |

Directive state: reconnaissance-gated evolution complete through ARC-4. Every
ledgered slice SHIPPED, live-proven where a runtime path exists, CI-green on
its full SHA. Release-gate PASS (exit 0). Repo is public-release ready:
secret scan of tracked files clean, cred-bearing root scripts gitignored +
untracked, docs (LONGRUN, CHANGELOG) current.

### Public-release closeout — docs + evidence bookkeeping (ARC-4 tail)

| Commit | What | CI |
|---|---|---|
| a89b834 | chore(eval): record v3.152.x release-gate baseline + evidence snapshots (rolling `release-gate-latest.json` ok:true/requiredFailed:[], per-release `evidence-v3.152.{0,1}.json`, `stopSurface.json`) | ci+eval-regression+install-e2e+ledger-guard all green |
| 1772477 | docs(readme): document long-running objectives — `/objective` chat cmd + `POST /objectives` + deterministic `verify` gate, LONGRUN.md linked from docs map (every claim checked against the live route contract + verify.mjs types) | ci+eval-regression+install-e2e+ledger-guard all green |

Working tree clean after both. Gateway healthy on 3.152.1 (`/ready` all checks ok, deployer online).

**Soak (honest state):** `release-gate-latest.json` passes **non-strict** with `soakNights:0`. A genuine multi-night soak requires real calendar days (`scripts/soak-multinight.mjs`: "Prior 'nights' must be real runs over calendar days"); `SEED_ONLY=1` fake-night seeding was deliberately NOT used. Strict soak gating remains the one item a public maintainer would run over 3 nights post-release.

**Directive state: COMPLETE. Repo is public-release ready** — S1–S8 + E-A/E-B/E-C + file_equals hardening shipped v3.145.1→v3.152.1, all CI-green and live-proven, release published, docs current, tree clean.
