{
  "name": "@xclaw/swarm-ext",
  "version": "0.1.0",
  "description": "XClaw Swarm Extension — isolated opt-in multi-agent orchestration module (vendored). OFF by default; enabled via xclaw cfg swarmExt.enabled. The xclaw core stays zero-dependency: these deps install ONLY inside src/swarm-ext/ (npm install --prefix src/swarm-ext).",
  "type": "module",
  "private": true,
  "main": "src/swarm/index.mjs",
  "scripts": {
    "test": "node --test test/swarm/*.test.mjs",
    "doctor": "node scripts/swarm-doctor.mjs"
  },
  "dependencies": {
    "express": "^4.18.0",
    "ioredis": "^5.3.0",
    "zod": "^3.22.0"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
